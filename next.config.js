/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
    images: {
        domains: ['www.google.com', 'coaching.papareact.com']
    }
}

module.exports = nextConfig
